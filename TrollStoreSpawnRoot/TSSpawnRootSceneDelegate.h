#import <UIKit/UIKit.h>

/**
 * TSSpawnRootSceneDelegate
 *
 * Scene delegate for TrollStore SpawnRoot.
 * Manages the lifecycle of the app's scenes on iOS 13+.
 */
@interface TSSpawnRootSceneDelegate : UIResponder <UIWindowSceneDelegate>

@property (nonatomic, strong) UIWindow *window;

@end
